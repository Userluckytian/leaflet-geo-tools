# Release Plan for v1.0

## 目标
将 `leaflet-geo-tools` 从 0.x 系列平滑迁移至 1.0。主要目标：
- 梳理并统一 `draw/` 与 `edit/` 中的公共逻辑（吸附、历史栈、几何校验、添加中点、拖动边/面等）。
- 保持现有 `edit/polygon.ts` 与 `edit/rectangle.ts` 的 API 兼容性（在 1.x 中尽量保持方法签名一致以便迁移）。
- 提供迁移指南与测试用例。

## 分步计划
1. 代码审查：标注 `draw/` 与 `edit/` 中可提取的公共函数和类型。 
2. 抽象层：提取 `BaseEditor` 中可复用的吸附与历史栈逻辑为 `core/` 模块。 
3. 迁移：将 `polyline`, `circle`, `markerPoint` 等基于 `draw/` 的工具逐步迁移到 `core/` 抽象上，确保行为一致。 
4. 测试：增加单元/集成测试覆盖吸附、撤销/重做、几何校验等关键场景。 
5. 文档：更新 README、示例以及 `CHANGELOG.md`。
6. 发布：当所有关键模块稳定后，将 `package.json` 版本号调整为 `1.0.0` 并发布。

## 里程碑
- M1: 完成公共 API 与类型定义（预计 1 周）
- M2: 将 `edit/` 与 `draw/` 的核心重构完成（预计 2 周）
- M3: 完成测试与示例验证（预计 1 周）
- M4: 发布 1.0（预计在 M1-M3 完成后）

## 迁移注意事项
- 在 1.0 前保持 `0.1.8` 为稳定基线，鼓励用户在迁移前先锁定依赖版本。
- 若 API 不可避免发生破坏性更改，请在 `README` 中提供 `v0 -> v1` 的迁移示例。

---
*自动提交：为响应 issue #1，添加发布计划与变更日志草案.*